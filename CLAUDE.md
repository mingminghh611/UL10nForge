# 项目开发规则（Unity 游戏汉化工具）

> 本文件是项目级 agent 规则，**每次开发/修复前必须阅读**。

## 0. 强制：开发前先查问题集

1. **开发/修复任何模块前**，先阅读 `docs/问题与解决方案集.md` 的目录 + 各章节标题，确认要改的模块是否有已记录问题。命中的条目必须对照「解决方案」与「检查点」执行，防止复现。
2. **任何新问题解决后**，必须：
   - 在 `docs/问题与解决方案集.md` 追加条目（现象 / 根因 / 解决方案 / 检查点）；
   - 写入 `C:\Users\mingming\.claude\projects\C--Users-mingming-Desktop-AI---unity------\memory\` 经验记忆 + 更新 `MEMORY.md` 索引；
   - 给新规则补回归测试。
3. **禁止复现**：一次解决的问题，后续新更新不得再次出现。回归测试是防线。

## 1. 核心原则

- **宁漏勿坏**：写回安全优先于翻译覆盖率。拿不准的写回宁可保留原文，绝不冒险破坏游戏。
- **识别宁严勿漏**：识别要彻底全面，但精确筛选真正需要翻译的文本。不需要翻的（引擎日志/署名/许可证/对象名/代码标识）不翻。
- **翻译质量 = 写回安全的一部分**：乱翻/幻觉译文写回后会破坏游戏（按键失灵/音乐消失/UI 消失）。质量治理与写回安全同等重要。
- **本地库分两处**：GUI 库 `~/.hanhua/projects/<md5>[:10]/project.db`；runner 库 `~/.hanhua_sweep/projects/<slug>/project.db`。

## 2. 写回安全要点（最高优先级，详见问题集 A 节）

- 按钮 `m_Name` 与文本同值结构 → 提取跳过 + 写回回退（双层）。
- FMOD/配置类写回必须走全量复制（externals 兄弟文件在场才能解析类名）。
- CSV 判定必须先于 YAML；yaml 分支行数守恒；JSON/CSV 结构守恒。
- 写回后跑 `writeback_audit`（确定性硬闸门 + 模型软复核）。
- 验证：changed 对象必须都是目标对象；`m_Name` 0 变化；音频对象 0 变化；字符串 diff 无语义无关译文。

## 3. 识别要点（详见问题集 B 节）

- 引擎字符串（il2cpp_sentence/format_template）标 skipped，不进翻译池。
- `_CODE_DRIVEN_METHODS`（SetText/SetActive）计入 code 信号，防对象名误译。
- 多语言词典组内只取第一张，键保真。
- 跳过是哑信号：扫描报告要有形态级 skipped 率告警。

## 4. 翻译/审核要点（详见问题集 C 节）

- 审核引用最终译文而非中间快照。
- 注入参考给模型必须与本条文本相关（术语按命中注入 ≤5 条）。
- 原文低质量/梗/自嘲豁免（维度 11），但真坏译文仍拦。
- 幻觉译文（含路径/文件名痕迹）拒绝重译。
- 术语词对必须带语境保护；键名强制保留。

## 5. 性能要点（详见问题集 E 节）

- 先找 O(N²)：逐条 SELECT、循环内 re.compile、每批全量扫。
- 慢翻译排查先查「自动翻译池是否有足够 is_actionable_translation 条目」，再查并发/进度。
- 新页面订阅 entriesChanged：重活进 Worker + 翻译中挂起。

## 6. 流程规则

- 版本号：大升级 0.X.0，小修复 0.X.1。
- **push 仅在用户明确要求时**；否则全本地提交。
- 免实机测试、免云端语义审核。
- KoiKoi 是真实游戏名，仅限本地分析，**绝不写进 README/repo**。
- 发布打包三通道：Full/Lite/Models，Lite 与 Full 唯一区别是 3 大模型。
- 记录模板规范见 `docs/记录模板规范.md`。

## 7. 术语

- 全链路：识别 → 翻译 → 审校 → 写回 → 发布验证。
- BUILTIN：内置权威译名映射，识别端与翻译端共用。
