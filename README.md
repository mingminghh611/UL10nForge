# UL10nForge · Unity 游戏智能汉化工作台

> **完全离线的 Unity 游戏汉化工作台**：识别 → 翻译 → 审校 → 写回，每一步都有确定性检查与证据留档。你得到的不是一次性的机器翻译，而是一份可以逐条复核、越用越准的汉化工程。

---

## 1. 项目简介

UL10nForge 把「汉化」做成一条**可验证的工程流水线**，而不是把文本丢给模型、翻完塞回去碰运气：

| 环节 | 做什么 | 怎么保证不翻坏 |
|---|---|---|
| ① 识别 | 扫描游戏目录，检测 Unity 版本与文本存放方式（Mono / IL2CPP / Addressables），未知形态**明示阻断**而非假报成功 | 识别报告 + 独立字节级普查对照，零盲区可验证 |
| ② 翻译 | 记忆直填 → 语境重排选参考 → 本地模型翻译 | 20+ 项确定性质量门（占位符 / 富文本 / 数字 / 按键名保护） |
| ③ 审校 | 4B 审核模型逐条判定，分 PASS / MINOR / MAJOR / CRITICAL 四级 | MAJOR / CRITICAL 带理由自动重译（≤2 轮收敛） |
| ④ 写回 | 按原格式写回游戏文件，重开比对逐条核对 | 四态闸门（文件 / 容器 / 对象 / 运行时）+ 原子发布 |
| ⑤ 发布 | 字体覆盖验证 + 发布副本验证 | 缺失码点如实报告，绝不「汉化了却全是口口」 |

**四个本地模型分工**（全本地推理，文本不出本机）：

| 模型 | 职责 | 档位 |
|---|---|---|
| Hy-MT2-1.8B | 翻译 | 轻量高速 |
| Qwen3.5-4B | 语义审核 | 深度把关 |
| Qwen3-Embedding-0.6B | 向量检索 | 相似记忆召回 |
| Qwen3-Reranker-0.6B | 语境重排 | 候选排序 |

- **隐私安全**：本地模式纯离线，不向任何云端发送文本；检测 / 提取 / 写回只发生在所选游戏目录内，写回前自动备份
- **越用越熟**：跨游戏经验记忆 + 术语库 + 语境库 + 知识库自动沉淀，翻过的内容直接复用
- **一键闭环**：双击启动器即用（内置 Python 3.12 + llama.cpp），或命令行全流程 headless 运行
- **智能上下文**：按游戏文本实时计算模型上下文（单条硬约束 + 不冗余），不再固定档位
- **加密可解**：内置 UnityCN 加密 AssetBundle 自动解密（暴力探测密钥，无需手动干预）
- **写回加固**：写回后 UnityFS 校验字段（hash/crc）检测与修复；IL2CPP metadata 任意变长写回
- **字体补全**：多字体合并 + 按译文需求集裁剪，中文字体缺字根治

> **关于作者**：本项目由一位**编程与游戏汉化的新手**借助 **AI 辅助**独立开发——大量代码与文档由 AI 参与生成、审查与迭代。如果你也是新手，这个项目本身就是一次完整的「AI 辅助开发」实践；如果你是资深开发者，欢迎指正与加入（见[第 10 节](#10-联系信息与致谢)）。

---

## 2. 安装与模型下载（必读）

> 工具本体不包含任何模型文件。**模型需要单独下载**，放到应用目录下的 `models/` 文件夹里。根据你的使用方式选对版本：

### 2.1 先判断：云端用户 or 本地用户？

| 使用方式 | 下载哪个包 | 需要模型吗 |
|---|---|---|
| **在线 API 模式**（工具内置支持：翻译 / 审核 / 检索三张云端 API 卡，重排恒本地） | 下载**不含模型**的轻量版（体积小、秒下） | ❌ 不需要本地模型——翻译 / 审核 / 检索走你配置的云端接口 |
| **本地离线模式**（默认，数据不出本机） | 下载**含全部模型**的完整版，或轻量版 + 模型包 | ✅ 需要全部四个模型（翻译 / 审核 / 检索 / 重排） |

> **在线 API 模式怎么用**：安装轻量版 → 启动 → 设置页「运行模式」选「在线 API」→ 依次填入翻译 / 审核 / 检索三个端点的 Base URL、Key、模型名（可各自指向不同服务）→ 测试通过即可。只有**重排**这一个 0.6B 轻量任务恒走本地，因此轻量版会附带这一个模型（见下文模型包说明）。

### 2.2 下载清单

**① 工具本体**（二选一）：

- `UL10nForge-<版本>-Lite.7z` —— **不含模型**，面向在线 API 用户（约几百 MB，含内置 Python + llama.cpp 运行时）
- `UL10nForge-<版本>-Full.7z` —— **含全部四个模型**，解压即用，面向本地离线用户（约 5.3 GB）

**② 模型包**（本地用户补模型用，可组合下载）：

| 文件 | 内容 | 大小 | 用途 |
|---|---|---|---|
| `models-translate-<版本>.7z` | Hy-MT2-1.8B-Q6_K.gguf | ≈1.4 GB | 翻译 |
| `models-review-<版本>.7z` | Qwen3.5-4B-Q4_K_M.gguf | ≈2.6 GB | 审核 |
| `models-embed-<版本>.7z` | Qwen3-Embedding-0.6B-Q8_0.gguf | ≈610 MB | 向量检索 |
| `models-rerank-<版本>.7z` | Qwen3-Reranker-0.6B.Q8_0.gguf | ≈610 MB | 语境重排（在线 API 模式也必需） |

**③ 发布页必读**：每个文件旁都附 **SHA256** 校验值，下载后务必核对（见 2.5）。

### 2.3 安装步骤（Windows）

**本地离线用户**：

1. 下载 **Full 版**（或 Lite 版 + 四个模型包）
2. 解压到**纯英文路径**（如 `D:\Games\UL10nForge`——工具链对非 ASCII 路径支持有限）
3. 把模型包解压出的 `.gguf` 文件全部放进应用目录的 `models/` 文件夹：
   ```
   UL10nForge/
   ├── models/            ← 模型放这里
   │   ├── Hy-MT2-1.8B-Q6_K.gguf
   │   ├── Qwen3.5-4B-Q4_K_M.gguf
   │   ├── Qwen3-Embedding-0.6B-Q8_0.gguf
   │   └── Qwen3-Reranker-0.6B.Q8_0.gguf
   ├── runtime/           ← 内置 Python + llama.cpp
   ├── main.py
   └── 启动UL10nForge.bat
   ```
   > **模型缺失会怎样**：缺失的模型会显示为「不可用」，翻译 / 审核 / 检索对应功能会降级或明确提示——不会静默跑错。四个模型齐了体验才完整。
4. 双击 `启动UL10nForge.bat`，首次启动自动拉起本地模型服务，就绪后进入工作台

**在线 API 用户**：

1. 下载 **Lite 版**并解压到纯英文路径
2. 从 Release 单独下载 `models-rerank-<版本>.7z`，解压出 `Qwen3-Reranker-0.6B.Q8_0.gguf` 放入 `models/`
3. 双击启动 → 设置页「运行模式」切到「在线 API」→ 配置翻译 / 审核 / 检索三个云端端点

**开发环境**（可选，从源码跑）：

```bash
pip install -r requirements.txt
python main.py
```

### 2.4 硬件要求（客观）

| 配置 | 体验 | 说明 |
|---|---|---|
| 无显卡 · 内存 8GB+ | ✅ 能跑（慢） | 全 CPU 运行：翻译短句约几秒一条；审核模型按需加载，首轮启动较慢 |
| 显存 4~6GB | ✅ 可用 | 审核模型（4B）上显卡，翻译模型（1.8B）由 CPU 分担——全自动，无需手动配置 |
| 显存 6~8GB | 👍 流畅 | 审核常驻显卡 + 翻译按需上显卡 |
| 显存 8GB+ | 🚀 舒适 | 全模型常驻显卡，速度最快 |

- **自动分担机制**：启动时探测本机显存与内存，自动分配每个模型「上显卡 / 部分层显卡 / 纯 CPU」——显存放不下时自动把部分层分给 CPU（`gpu_layers` 部分卸载），不会因显存不足直接失败
- **纯 CPU 完全可用**：没有 N 卡（或 AMD 显卡）也能汉化，只是慢；内存建议 8GB 以上（低于 8GB 会自动按需加载模型，勉强可跑但更慢）
- 两个 0.6B 小模型（语境重排 / 向量检索）恒定 CPU 运行，不占显存

**设置页关键选项（新手看这里）**：

| 选项 | 怎么选 |
|---|---|
| 模型运行方式（GPU/CPU） | 新手一律选**「自动（推荐）」**：有显卡就优先用显卡，显存放不下自动分部分层给 CPU；「GPU」强制全显存（仅 8GB+ 显存选它）；「CPU」纯处理器运行（没显卡 / 显存不足 2GB 时选） |
| 并发槽位 | 默认「自动 · 单槽」最省心。显卡一次只能干一件事，多槽只是排队交替，速度不成倍变快、显存却成倍占用 |
| 上下文长度 | 默认 8192 已覆盖常规游戏文本；内存紧张可选 4096 省内存，几乎不影响翻译质量 |
| 每批条数 | 短文本为主（按钮 / 标签）选 16-32；长文本多（对话 / 文档）选 4~8——打包越多，每条分到的翻译空间越少 |

### 2.5 校验文件完整（SHA256）

Release 页面每个文件旁附 SHA256。下载后可核验：

```powershell
Get-FileHash .\UL10nForge-0.37.0-Full.7z -Algorithm SHA256
# 比对结果是否与发布页一致
```

### 2.6 帮助与支持

| 渠道 | 说明 |
|---|---|
| 问题反馈 | 本仓库 Issues（附复现步骤 + 日志） |
| 交流群 | QQ 群 **931708916**（日常交流、问题互助、测试反馈首选） |
| 文档导航 | 见[第 12 节](#12-文档导航) |

---

## 3. 快速开始

### 3.1 GUI 五步

| 步骤 | 操作 |
|---|---|
| ① | 拖入游戏文件夹，自动检测 Unity 版本与文本存放方式 |
| ② | 扫描全部可改文本（未知形态告警留档） |
| ③ | 设置页维护术语库（角色名 / 地名 / 物品名，翻译自动遵守） |
| ④ | 翻译页一键批量翻译，审校页逐条确认 |
| ⑤ | 写回 → 开始游戏 → 重开核对 |

> 拖入的文件夹若打包了**多个 Unity 游戏**（一个根目录多个玩家布局），会弹出选择框让你指定要翻译的那个——不同游戏自动隔离到独立项目库。

**命令行排查（免界面全流程 + 全环节记录）**：

```bash
python scripts/all_record_runner.py "<游戏目录>"
```

### 3.2 给新手的建议

1. **先挑简单的游戏练手**——视觉小说、2D 独立游戏文本量大但结构规整；3D 大作常加密 / 运行时拼接，门槛高
2. **游戏路径避免中文与特殊字符**——工具链对非 ASCII 路径支持有限
3. **分章节汉化**——先跑一遍生成底稿，游玩时在审校页精修保存，下次翻译直接复用（记忆直填）
4. **专有名词先沉淀**——术语库里的角色名 / 地名 / 物品名会被翻译自动遵守，并跨游戏复用
5. **「翻译失败」不等于坏**——质量门拦截的是「可能翻坏」的译文，审校页可逐条看原因、手动改

---

## 4. 工作流程与示例

### 4.1 流水线

| 步骤 | 做什么 |
|---|---|
| ① 识别 | 检测 Unity 版本与文本存放方式（Mono / IL2CPP / Addressables），未知形态 blocked 不假报 |
| ② 翻译 | 记忆直填 → 语境重排选参考 → 模型翻译 → 质量门 → 四级语义审核 → 反馈重译 |
| ③ 审校 | 4B 审核模型逐条判定，MAJOR / CRITICAL 带理由自动重译（≤2 轮），未收敛阻塞留人工 |
| ④ 写回 | 按原格式写回，重开比对逐条核对；四态闸门 + 原子发布 |
| ⑤ 发布 | 字体覆盖验证 + 发布副本验证 |

### 4.2 命令行全流程示例

```bash
# 一键闭环（识别→翻译→审校→写回→验证，全程留档）
python scripts/all_record_runner.py "D:\Games\MyGame"

# 全量测试
python -m pytest -q
```

### 4.3 开发集成示例（BatchTranslator）

```python
from hanhua.core.batch_translator import BatchTranslator
from hanhua.core.translator import create_client

client = create_client(api)                     # 本地 llama-server 客户端
translator = BatchTranslator(
    client, batch_size=20, concurrency=1,
    system_prompt=build_system_prompt(profile, ""),
    glossary=active_terms,                      # 术语按条目命中注入
    vector_recall=knowledge_retrieval.vector_recall,   # 向量相似召回 Top-3
)
stats = translator.run(entries)                 # done/failed/requests
```

- 翻译前自动探测服务端实际上下文，超出预算自动整批降级逐条
- 术语 / 知识 / 向量参考全部**按条目检索命中注入**，提示词只含与本条原文相关的内容

---

## 5. 项目结构和文件组织

### 5.1 总体架构

```
界面层（PySide6 四页工作台：概览 → 审校 → 翻译 → 设置）
    │
核心层（与界面解耦）：提取（UnityPy/dnfile/IL2CPP）→ 翻译（记忆直填→语境重排→模型→质量门→四级审核→重译链）
    │                    → 写回（四态闸门+重开验证）→ 知识（术语库/语境库/AgentMemory/向量库）
    │
运行时（llama.cpp 四模型）：Hy-MT2-1.8B 翻译 · Qwen3.5-4B 审校 · Qwen3-Embedding 0.6B 检索 · Qwen3-Reranker 0.6B 重排
```

### 5.2 目录结构

```
hanhua/
├── core/                  # 核心层：解析/翻译/质量/审核/存储/写回
│   ├── unity/             # 提取写回（UnityPy/dnfile/IL2CPP）
│   ├── quality.py         # 质量门（20+ 硬规则 + 失败原因分类）
│   ├── batch_translator.py# 批量翻译主流程
│   ├── reviewer.py        # 四级语义审核 + 反馈重译闭环
│   ├── risk_gate.py       # 审核分流（风险信号裁决）
│   ├── agent_memory.py    # 经验记忆（证据驱动晋升/退休）
│   └── memory.py knowledge.py glossary.py   # 存储/知识库/术语库
├── ui/                    # PySide6 工作台界面
├── scripts/               # 命令行工具（打包/排查/审计）
├── tests/                 # 2800+ 测试
├── models/                # 四个模型 GGUF（本地用户自行放置）
├── runtime/               # 内置 Python 3.12 + llama.cpp（发行包）
└── resources/ tools/ fonts/   # 工具清单/外部工具/字体链
```

### 5.3 关键文件用途

| 文件/目录 | 用途 |
|---|---|
| `hanhua/core/batch_translator.py` | 批量翻译主流程：记忆直填、语境参考、质量门、反馈重译链 |
| `hanhua/core/reviewer.py` | 四级语义审核 + 理由驱动的针对性重译 |
| `hanhua/core/quality.py` | 确定性质量门（占位符/标签/数字/按键名保护等 20+ 规则） |
| `scripts/all_record_runner.py` | 命令行一键闭环（headless 生产入口） |
| `scripts/package.py` | 发行包打包（白名单 + 隐私扫描 + 完整性验证） |
| `tests/` | 2800+ 测试（全流程回归保障） |

---

## 6. 质量保障

每条译文过**三道关 + 审核环**：

1. **记忆关** —— 翻过的一模一样文本直接复用，前后一致（跨游戏共享）
2. **规则关** —— 20+ 确定性检查：占位符（`{player}` / `%s`）不丢、富文本标签不坏、数字不误改、按键名保护、半翻译拦截
3. **经验关** —— AgentMemory 只收质量门通过的译文，证据驱动晋升；术语库 / 语境库 / 知识库注入翻译上下文
4. **审核环** —— Qwen3.5-4B 十维审查分四级，MAJOR / CRITICAL 带理由自动重译（≤2 轮收敛）

---

## 7. 支持范围（客观）

| 文本来源 | 提取 | 写回 |
|---|---|---|
| 本地化文件（JSON/CSV/TSV/XML/TXT/INI） | ✅ | ✅ 保留编码与换行 |
| TextAsset（含老 Unity 4.x/5.x） | ✅ | ✅ 可变长重建 + 重开验证 |
| MonoBehaviour 序列化字符串 | ✅ | ✅ 对象级重开验证 |
| Addressables（Unity Localization 表） | ✅ | ✅ Entry ID 写回 + CRC 同步 |
| Mono 程序集（C# #US 堆） | ✅ | ✅ 受原容量限制，超长截断并报告 |
| IL2CPP metadata | ✅ | ✅ 仅验证过 v24/27/29/31/39 |

**边界**：已处理样本覆盖多种 Unity 版本与打包方式，但**不等于所有 Unity 游戏通用**；不可汉化的（运行时拼接 / 加密 / 服务器下发 / 贴图文字 / 位图字体）明确阻断不假报；字体注入仅限 Windows x64 Mono。

### 字体（口口乱码）说明

替换字体 + 完整性验证链：写入游戏的字体需通过逐码点验证，缺失码点如实报告而非静默替换——防止「汉化了但全是口口」的假成功。

---

## 8. 当前局限（客观）

> 本项目仍处于早期阶段，以下问题如实告知，请按需评估：

- **识别不全**：拼接 / 加密 / 服务器下发 / 贴图内文字无法识别（需 OCR）；未知文本形态可能漏识别（有告警留档，但不保证全覆盖）——部分游戏的文本可能提不全
- **翻译质量不高**：1.8B 属小模型，复杂句 / 文学性表达质量有限；多义词易错译——4B 审核能拦一部分语义错译，但误判与漏判并存，需人工确认兜底，译文整体质量与专业汉化组有差距
- **写回可能有 bug**：同类未知引擎配置仍有风险；固定容量写回译文过长会截断；写回后**按键 UI 失灵、游戏卡住无法推进**等逻辑性问题可能发生（已有多层防护——对象名保护 / 事件绑定保护 / 写回前逻辑审计，但无法保证所有游戏零问题）——建议写回前备份原游戏文件，发现问题在审校页处理或反馈 Issue
- **验证局限**：闭环验证到「重开比对」为止，不做实机测试——UI 溢出、字体渲染、按键失灵等运行期问题可能漏检

---

## 9. 贡献指南

### 9.1 一个人的项目，期待更多双眼睛

> 本项目由作者**一人独立开发与维护**，没有外部合作者。但这不代表封闭——恰恰相反：一个人的项目终究有盲区，多一双眼睛，就少一处漏网。

如果你对这个项目感兴趣，以下任何一种参与都无比珍贵：

| 参与方式 | 适合谁 | 怎么做 |
|---|---|---|
| 反馈真实样本 | 任何用过工具的人 | 提交 Issue：附游戏版本、文本形态、出错环节 |
| 提交问题 | 遇到 bug 的用户 | Issue 带复现步骤 + 日志 |
| 补测试 / 文档 | 有开发经验的伙伴 | Fork → 本地开发 → 提交 Pull Request |
| 参与核心开发 | 想长期一起做的人 | 联系作者（见[第 10 节](#10-联系信息与致谢)） |

### 9.2 如何为项目做出贡献

1. **了解项目** —— 阅读 README 与 `docs/` 设计文档，理解目标、架构与设计原则
2. **找到贡献机会** —— 查看 Issues：可修复的缺陷、待实现的计划项
3. **贡献代码** —— Fork → 本地开发 → 提交 Pull Request

### 9.3 代码提交标准

- 代码必须符合项目现有风格（注释密度、命名、结构对齐）
- **提交的代码必须通过全量测试**（`python -m pytest -q`）
- 新功能必须附带测试（2800+ 测试是质量保障的基石）
- 提交信息遵循现有规范（`feat(core):` / `fix(quality):` / `docs(record):` 等前缀 + 中文描述）

### 9.4 提交问题与拉取请求的流程

| 方面 | 提交问题（Issue） | 提交拉取请求（PR） |
|---|---|---|
| 标题 | 明确、具体（如「质量门误判：XX 文本被拦截」） | 清晰、描述改动目的 |
| 描述 | 复现步骤 + 预期/实际行为 + 相关日志 | 改动内容 + 必要性 + 测试结果 |
| 附加 | 游戏样本特征（版本 / 形态）、截图 | 说明测试覆盖情况 |

---

## 10. 联系信息与致谢

### 10.1 联系信息

如有问题、建议，或**希望加入项目一起开发**，欢迎通过以下渠道联系：

| 渠道 | 说明 |
|---|---|
| 交流群 **931708916** | QQ 群，日常交流、问题互助、测试反馈首选 |
| 本仓库 Issues | 附复现步骤与相关日志（bug 反馈） |

### 10.2 致谢

本项目由作者一人独立开发。每一轮排查与修复都来自真实游戏样本的实证反馈——感谢这些游戏，没有它们，就不会有今天的质量门与修复清单。

**期待加入**：无论你是想反馈一个真实样本、补一条测试、写一段文档，还是参与核心功能开发，都欢迎联系作者。一个人的项目终究有盲区，多一双眼睛，就少一处漏网。

---

## 11. 版本

**0.37.0**（2026-08-31）：识别文本量收敛 + 独立 JSON 数据区过滤 + 语境识别直连——① 游戏语境识别改本地直连 llama-server（独立 system 消息 / 无 response_format / 用 4B 审核模型），识别结果真正注入翻译与审校 prompt（此前 save_game_context 未同步 context_* 字段，识别零效果）；② 识别文本量上升排查：8 More Lives 17389 条 json pending 中 3032 条是数据区误识别（hex 颜色 / 数值公式 / 资源引用枚举 / 数组下标枚举），新增 `_is_json_data_area` 结构判定（按 inner_path 拦截，不能按值拦截——ARCTIC/STRIKE 在数据区与显示区同时出现）；③ 独立 `.json` 文本文件（游戏 Data/ 下的字幕/词典/设置文件）数据区叶子此前漏过资产内嵌 TextAsset 的过滤闸门，现共用同一规则过滤，且提取期 skipped 条目写回侧拒绝写入（宁漏勿坏）；④ **Mono #US 调试/结构串收敛**（alisa-demo 地毯式实证，DLL pending 184→3）：`MonoBehaviour.print`（旧版 Debug.Log，~120 条开发日志经句子启发式误放行）/ `AnimatorStateInfo.IsName`（动画状态名）/ `LayerMask.GetMask(params string[])`（图层名，补 newarr/stelem 数组元素证明链）/ `PlayerPrefs.Get*`（持久化键名）/ 输入绑定按键名标签（JS_ButtonN/'Left Shift' 虽被证明为 UI 文本，翻译后绑定失效——硬跳过）全部拦截，防翻译断动画/物理/输入配置；⑤ 首个正式分发版，拆分为「轻量版（不含模型）+ 模型包」双通道发行（在线 API 用户下载轻量版即可，本地离线用户下载完整版或轻量版 + 四个模型包，模型统一放 `models/`）；⑥ README 全面重写，新增安装与模型下载章节；⑦ 清理仓库过期与多余文件（运行日志 / 临时数据库 / 游戏闭环记录样本），删除仓库内多余脚本。

**0.36.x**（2026-08-26 ~ 2026-08-29）：写回可靠性收官季——① 全游戏静音根因修复：FMOD 配置类三层防线（识别登记 + 头部解析 + 写回现解析兜底）；② 写回按键失灵哑问题系统性拦截：UnityEvent 回调 / FMOD 路径 / 输入轴名三线修复；③ 持久连接误用 `with` 块致 122 条 request_error 修复（httpx.Client 退出即 CLOSED 不可复用）；④ 本地翻译提速：GPU 默认双槽 + HTTP 持久连接复用；⑤ 审核端口清场只杀 llama-server，防误杀无关服务；⑥ 写回侧 m_Name 兜底（rawstr 定位器指向对象名时宁漏勿坏）；⑦ 语义回退原文从翻译记忆撤销，跨游戏阻断坏译文复命中断链。

**0.35.0**（2026-08-22）：设置与术语库 UI 重做 + 游戏语境 + 稳定性修复——① 设置页重构（信息分级 / 区块留白 / 说明文案独立成分类）；② 术语库页重做（分组 / 预览 / 快捷引用）；③ 游戏语境（术语 / 对象名 / 人称等语境自动注入翻译，配套对话框 UI）；④ 稳定性——审校 TRANSPORT_ERROR 根因修复（中文 Windows 下 netstat 输出 GBK 解码崩溃）、写回复制误拒修复（Windows ctime 语义差异 → 大文件指纹改 size+mtime 双因子）。

**0.34.0**（2026-08-21）：翻译工作台体验统一——① 快译小工具提示词回显根治；② 翻译页运行区重做为左右双栏；③ 全链路进度条 3-3-3-1 统一（翻译 / 审校判定 / 审校处置 / 写回，单调不倒退）；④ 配色与动效统一（实时处理流状态按阶段语义着色，reduced-motion 直切）。

**0.33.0**（2026-08-20）：体验四连——扫描进度条（百分比 + 渐变色实时显示）、概览页任务流水线五格等宽重做、审校页来源文件筛选器、游戏档案卡重做与档案精简。

**0.32.0**（2026-08-20）：性能与安全大修——扫描六大根因修复（O(N²) 聚集 / 2 万次 DB commit / GUI GIL 饿死，实测扫描时间与峰值内存大幅下降）、翻译写回链路性能复盘、写回安全审计 8 项修复、GUI 多游戏目录选择框、跳过率告警（识别哑信号可见化）。

**0.31.0**（2026-08-18）：字体链路加固 + 单字体收敛——多容器补丁目标定位、TMP SDF bundle 运行时兜底（按 Unity 版本部署，保留原字体 fallback）、字体收敛为单一 Noto Serif CJK SC Medium（多字体 / 多档位收敛）、GUI 移除字体档位设置。

**0.30.0**（2026-08-15，首个公开测试版）：识别 / 翻译 / 审校 / 写回 / 发布五阶段流水线重做、审校管线四根因修复（术语提示污染 / 一致性杀重译 / 多行候选 / 批审幻觉）、对象名保护、写回字体门默认候选放行、翻译工具页纯翻译精简、审校页 AI 翻译填充、批量翻译进度按真实条数汇报。

**更早版本**：0.26.x 四模型本地推理 + 审核闭环 + 语境库 / 向量 / 重排 + UI 重构；0.25.x 第九轮排查 + AgentMemory 上线；0.24.x 写回校验闸门与识别证据分层；0.22.x 语义审核基础设施；0.20.x 六库知识库；0.11.0 发行包内置 Python 3.12，免安装。

---

## 12. 文档导航

完整设计文档与实施记录见本仓库 `docs/` 目录（发行包不含开发文档）：

| 文档 | 内容 |
|---|---|
| 地毯式排查升级计划 | 排查流程与修复清单（F1-F25+） |
| 翻译质量保障系统实施计划 | 质量体系设计与审核闭环架构 |
| 知识库体系 · 记录模板规范 | 知识沉淀与记录标准 |
| Unity 文本提取 · 写回回归验证指南 | 提取与安全写回方法 |

---

## 13. 许可证

### 13.1 项目许可声明

本项目当前以个人工具形式维护，源码许可待公开分发时正式确定（届时随仓库提供 `LICENSE` 文件）；在此之前，请在获得作者明确许可后使用、修改与再分发。

### 13.2 开源组件与许可

本工具基于以下开源组件构建（均允许自由使用与再分发；发行包已包含对应许可文本）：

| 组件 | 许可 |
|---|---|
| llama.cpp（推理运行时） | MIT |
| Qwen3 系列模型（4B/0.6B） | Apache-2.0 |
| UnityPy（资产解析） | MIT |
| Il2CppDumper（IL2CPP 元数据） | MIT |
| bmfont（位图字体生成） | 免费软件 |
| PySide6 / Qt（界面） | LGPL-3.0 |
| Noto Serif CJK SC（中文字体回退链，单字体收敛） | SIL OFL-1.1 |

> **合规说明**：本工具不包含、不复制任何游戏的资源与文本；汉化产物仅针对用户本机拥有的游戏副本。实测游戏均为用户本机自购 / 免费副本，本仓库不提供、不传播任何游戏本体或破解内容；游戏文本样本与闭环记录仅保留在本地，不入仓库。

---

## 14. 未来计划

**近期（0.3x）——核心功能聚焦**

1. **识别能力增强**：运行时拼接 / 动态文本跟踪识别、Addressables 深层扫描补强、贴图内文字 OCR 通道、未知形态告警改进
2. **写回可靠性**：引擎配置形态清单化保护、固定容量截断改进、重开验证覆盖面扩展
3. **翻译质量**：审校反馈重译链优化、术语库 / 语境库注入增强

**中期**

- 多语言对支持（当前英→中，扩展日→中、韩→中）
- 更大翻译模型档位（7B 级，按硬件自动选择）
- 自动截图比对验证（替代实机测试）
- 翻译文件导入 / 导出（CSV/JSON），对接汉化组协作与人工校对工作流

**远期**

- GitHub 发布页 + 自动更新检查（含完整许可清单随包分发，届时确定本项目 LICENSE）
- 术语库 / 语境库社区共享通道（匿名化导出）
- 插件化工具链（第三方提取 / 写回插件接口）

> 计划按实证与用户反馈滚动调整。
