# 「让 Unity 游戏汉化从“翻译”走向“本地化”」

> **完全离线的 Unity 游戏汉化工作台**：识别 → 翻译 → 审校 → 写回，每一步都有确定性检查与证据留档。你得到的不是一次性的机器翻译，而是一份可以逐条复核、越用越准的汉化工程。

---

## 1. 引言

> "人们只有用心去看，才能看到真实。事物的真实本质是肉眼无法看到的。"
> —— Antoine de Saint-Exupéry《小王子》

一个出色的 README 是项目与访问者之间的第一座桥。这份文档想让你在三分钟内搞清楚三件事：**这个工具做什么、怎么用、以及为什么值得试试**。

汉化不是「把原文丢给模型、翻完塞回去」这么简单。文本从哪来（识别）、哪些能碰哪些碰了会坏游戏（分析）、怎么翻不坏（质量门）、怎么放回原格式（写回）、怎么越翻越准（经验积累）——UL10nForge 把每一步都做成**可验证的工程**，而不是依赖模型碰运气。

> **关于作者**：本项目由一位**编程与游戏汉化的新手**借助 **AI 辅助**独立开发——大量代码与文档由 AI 参与生成、审查与迭代。如果你是资深开发者或汉化老手，欢迎不吝指正（见[第 11 节](#11-联系信息与致谢)）；如果你也是新手，这个项目本身就是一次完整的「AI 辅助开发」实践。

## 2. 项目描述

### 2.1 一句话描述

**UL10nForge 是一个完全离线的 Unity 游戏汉化工作台**：四模型分工（翻译 / 审核 / 重排 / 检索）在本地运行，译文经过确定性质量门与四级语义审核后按原格式安全写回游戏文件，并把每一次翻译的经验沉淀为可跨游戏复用的记忆与知识库。

### 2.2 特色

| 方面 | 说明 |
|---|---|
| 可接入性 | 零配置发行包：内置 Python 3.12 与 llama.cpp，解压双击即用 |
| 可维护性 | 核心层与界面解耦，全环节留档可审计，2800+ 测试护航 |
| 隐私安全 | 纯本地离线，文本不上传任何服务器——无额度、无隐私顾虑 |

- **四模型分工**：1.8B 翻译 + 4B 语义审核 + 0.6B 语境重排 + 0.6B 向量检索——翻译、把关、查证各有专用模型
- **规则护航**：模型负责翻译，规则负责「不翻坏」——每条译文过 20+ 项确定性质量门（占位符、富文本、数字、按键名保护…）
- **深度审核闭环**：译文分 PASS / MINOR / MAJOR / CRITICAL 四级审查，错误带理由自动重译，再审收敛
- **安全写回**：四态闸门（文件 / 容器 / 对象 / 运行时）+ 重开比对验证，不假报成功
- **越用越熟**：跨游戏经验记忆 + 术语库 + 语境库 + 知识库自动沉淀，翻过的内容直接复用
- **识别全面**：识别报告含独立字节级普查对照（零盲区可验证）——155 款游戏实测零未解释缺口；自动识别自带中文语言包的游戏（无需翻译）；大量候选串被跳过时主动告警（跳过率 ≥80% 时提示，识别哑信号可见化）
- **加密可解**：内置 UnityCN 加密 AssetBundle 自动解密（暴力探测密钥，无需手动干预）
- **智能上下文**：按游戏文本实时计算模型上下文（单条硬约束 + 不冗余），不再固定档位
- **写回加固**：写回后 UnityFS 校验字段（hash/crc）检测与修复（patchcrc 等价）；IL2CPP metadata 任意变长写回
- **字体补全**：多字体合并 + 按译文需求集裁剪（缺字自动补全，中文字体缺字根治）

### 2.3 能力路线

- 提取：Unity 容器（UnityPy）→ 加密解密（UnityCN）→ Mono #US 堆证明链 → IL2CPP metadata（提取 + 写回）→ 松散文本 / 对话脚本 / 图数据
- 写回：静态写回 + 校验修复 + 重开验证 + 逻辑层审计 + 原子发布
- 质量：质量门 20+ 项 + 语义审核四级 + 反馈重译 + 术语/知识沉淀

## 2.5 界面预览（开发中）

> 以下为当前开发版本的界面预览截图，界面仍在持续优化中，最终效果以发布版为准。

| 预览 1 | 预览 2 |
|---|---|
| ![预览 1](docs/preview/preview-1.png) | ![预览 2](docs/preview/preview-2.png) |

| 预览 3 | 预览 4 |
|---|---|
| ![预览 3](docs/preview/preview-3.png) | ![预览 4](docs/preview/preview-4.png) |

---

## 3. 安装和使用说明

### 3.1 下载

**GitHub Releases 分卷下载**

在 [Releases 页面](https://github.com/fkjy-123/UL10nForge/releases/tag/new-edition)) 

### 3.2 安装步骤

**发行包（零配置，推荐）**：

1. 解压到纯英文路径（如 `D:\Games\UL10nForge`——工具链对非 ASCII 路径支持有限）
2. 双击 `启动UL10nForge.bat`
3. 首次启动自动拉起本地模型服务，就绪后进入工作台

**开发环境**：

```bash
pip install -r requirements.txt
python main.py
```

**硬件要求（客观）**：

| 配置 | 体验 | 说明 |
|---|---|---|
| 无显卡 · 内存 8GB+ | ✅ 能跑（慢） | 全 CPU 运行：翻译短句约几秒一条；审核模型按需加载，首轮启动较慢 |
| 显存 4~6GB | ✅ 可用 | 审核模型（4B）上显卡，翻译模型（1.8B）由 CPU 分担——全自动，无需手动配置 |
| 显存 6~8GB | 👍 流畅 | 审核常驻显卡 + 翻译按需上显卡 |
| 显存 8GB+ | 🚀 舒适 | 全模型常驻显卡，速度最快 |

- **自动分担机制**：工具启动时探测本机显存与内存，按上表自动分配每个模型「上显卡 / 部分层显卡 / 纯 CPU」——显存放不下时自动把部分层分给 CPU 分担（`gpu_layers` 部分卸载），不会因显存不足直接失败
- **纯 CPU 完全可用**：没有 N 卡（或 AMD 显卡）也能汉化，只是速度慢；内存建议 8GB 以上（低于 8GB 会自动按需加载模型，勉强可跑但更慢）
- 两个 0.6B 小模型（语境重排/向量检索）恒定 CPU 运行，不占显存

**设置页关键选项（新手看这里）**：

| 选项 | 怎么选 |
|---|---|
| 模型运行方式（GPU/CPU） | 新手一律选**「自动（推荐）」**：有显卡就优先用显卡，显存放不下自动分部分层给 CPU 分担；「GPU」强制全显存（仅 8GB+ 显存选它）；「CPU」纯处理器运行（没显卡/显存不足 2GB 时选，能跑但慢） |
| 并发槽位 | **默认「自动 · 单槽」**最省心。显卡一次只能干一件事，多槽只是排队交替，速度不成倍变快、显存却成倍占用 |
| 上下文长度 | **默认 8192** 已覆盖常规游戏文本；内存紧张可选 4096 省内存，几乎不影响翻译质量 |
| 每批条数 | 短文本为主（按钮/标签）选 16-32；长文本多（对话/文档）选 4~8——打包越多，每条分到的翻译空间越少 |

### 3.3 快速开始（GUI 五步）

| 步骤 | 操作 |
|---|---|
| ① | 拖入游戏文件夹，自动检测 Unity 版本与文本存放方式 |
| ② | 扫描全部可改文本（未知形态告警留档） |
| ③ | 设置页维护术语库（角色名/地名/物品名，翻译自动遵守） |
| ④ | 翻译页一键批量翻译，审校页逐条确认 |
| ⑤ | 写回 → 开始游戏 → 重开核对 |

> 拖入的文件夹若打包了**多个 Unity 游戏**（一个根目录多个玩家布局），会弹出选择框让你指定要翻译的那个——不同游戏自动隔离到独立项目库。

**命令行排查（免界面全流程 + 全环节记录）**：

```bash
python scripts/all_record_runner.py "<游戏目录>"
```

### 3.4 给新手的建议

1. **先挑简单的游戏练手**——视觉小说、2D 独立游戏文本量大但结构规整；3D 大作常加密/运行时拼接，门槛高
2. **游戏路径避免中文与特殊字符**——工具链对非 ASCII 路径支持有限
3. **分章节汉化**——先跑一遍生成底稿，游玩时在审校页精修保存，下次翻译直接复用（记忆直填）
4. **专有名词先沉淀**——术语库里的角色名/地名/物品名会被翻译自动遵守，并跨游戏复用
5. **「翻译失败」不等于坏**——质量门拦截的是「可能翻坏」的译文，审校页可逐条看原因、手动改

### 3.5 帮助与支持

| 渠道 | 说明 |
|---|---|
| 问题反馈 | 本仓库 Issues（附复现步骤 + 日志） |
| 文档导航 | 见[第 13 节](#13-文档导航) |
| 开发记录 | 本地 `docs/all record/` 保留各游戏闭环三件文档（不入库，见[第 12 节](#12-许可证)合规说明） |

## 4. 工作流程与示例

### 4.1 流水线

| 步骤 | 做什么 |
|---|---|
| ① 识别 | 检测 Unity 版本与文本存放方式（Mono/IL2CPP/Addressables），未知形态 blocked 不假报 |
| ② 翻译 | 记忆直填 → 语境重排选参考 → 模型翻译 → 质量门 → 四级语义审核 → 反馈重译 |
| ③ 审校 | 4B 审核模型逐条判定，MAJOR/CRITICAL 带理由自动重译（≤2 轮），未收敛阻塞留人工 |
| ④ 写回 | 按原格式写回，重开比对逐条核对；四态闸门 + 原子发布 |
| ⑤ 发布 | 字体覆盖验证 + 发布副本验证 |

### 4.2 命令行全流程示例

```bash
# 一键闭环（识别→翻译→审校→写回→验证，全程留档）
python scripts/all_record_runner.py "D:\Games\MyGame"

# 全量测试
python -m pytest -q
```

### 4.3 开发集成示例（BatchTranslator）

```python
from hanhua.core.batch_translator import BatchTranslator
from hanhua.core.translator import create_client

client = create_client(api)                     # 本地 llama-server 客户端
translator = BatchTranslator(
    client, batch_size=20, concurrency=1,
    system_prompt=build_system_prompt(profile, ""),
    glossary=active_terms,                      # 术语按条目命中注入
    vector_recall=knowledge_retrieval.vector_recall,   # 向量相似召回 Top-3
)
stats = translator.run(entries)                 # done/failed/requests
```

- 翻译前自动探测服务端实际上下文，超出预算自动整批降级逐条
- 术语/知识/向量参考全部**按条目检索命中注入**，提示词只含与本条原文相关的内容

## 5. 项目结构和文件组织

### 5.1 总体架构

```
界面层（PySide6 四页工作台：概览 → 审校 → 翻译 → 设置）
    │
核心层（与界面解耦）：提取（UnityPy/dnfile/IL2CPP）→ 翻译（记忆直填→语境重排→模型→质量门→四级审核→重译链）
    │                    → 写回（四态闸门+重开验证）→ 知识（术语库/语境库/AgentMemory/向量库）
    │
运行时（llama.cpp 四模型）：Hy-MT2-1.8B 翻译 · Qwen3.5-4B 审校 · Qwen3-0.6B 重排 · Qwen3-0.6B 向量
```

### 5.2 目录结构

```
hanhua/
├── core/                  # 核心层：解析/翻译/质量/审核/存储/写回
│   ├── unity/             # 提取写回（UnityPy/dnfile/IL2CPP）
│   ├── quality.py         # 质量门（20+ 硬规则 + 失败原因分类）
│   ├── batch_translator.py# 批量翻译主流程
│   ├── reviewer.py        # 四级语义审核 + 反馈重译闭环
│   ├── risk_gate.py       # 审核分流（风险信号裁决）
│   ├── agent_memory.py    # 经验记忆（证据驱动晋升/退休）
│   └── memory.py knowledge.py glossary.py   # 存储/知识库/术语库
├── ui/                    # PySide6 工作台界面
├── scripts/               # 命令行工具（打包/排查/审计）
├── tests/                 # 2800+ 测试
├── models/                # 四模型 GGUF（发行包）
├── runtime/               # 内置 Python 3.12 + llama.cpp（发行包）
└── resources/ tools/ fonts/   # 工具清单/外部工具/字体链
```

### 5.3 关键文件用途

| 文件/目录 | 用途 |
|---|---|
| `hanhua/core/batch_translator.py` | 批量翻译主流程：记忆直填、语境参考、质量门、反馈重译链 |
| `hanhua/core/reviewer.py` | 四级语义审核 + 理由驱动的针对性重译 |
| `hanhua/core/quality.py` | 确定性质量门（占位符/标签/数字/按键名保护等 20+ 规则） |
| `scripts/all_record_runner.py` | 命令行一键闭环（headless 生产入口） |
| `scripts/package.py` | 发行包打包（白名单 + 隐私扫描 + 完整性验证） |
| `tests/` | 2800+ 测试（全流程回归保障） |

## 6. 质量保障

每条译文过**三道关 + 审核环**：

1. **记忆关** —— 翻过的一模一样文本直接复用，前后一致（跨游戏共享）
2. **规则关** —— 20+ 确定性检查：占位符（`{player}`/`%s`）不丢、富文本标签不坏、数字不误改、按键名保护、半翻译拦截
3. **经验关** —— AgentMemory 只收质量门通过的译文，证据驱动晋升；术语库/语境库/知识库注入翻译上下文
4. **审核环** —— Qwen3.5-4B 十维审查分四级，MAJOR/CRITICAL 带理由自动重译（≤2 轮收敛）

## 7. 支持范围（客观）

| 文本来源 | 提取 | 写回 |
|---|---|---|
| 本地化文件（JSON/CSV/TSV/XML/TXT/INI） | ✅ | ✅ 保留编码与换行 |
| TextAsset（含老 Unity 4.x/5.x） | ✅ | ✅ 可变长重建 + 重开验证 |
| MonoBehaviour 序列化字符串 | ✅ | ✅ 对象级重开验证 |
| Addressables（Unity Localization 表） | ✅ | ✅ Entry ID 写回 + CRC 同步 |
| Mono 程序集（C# #US 堆） | ✅ | ✅ 受原容量限制，超长截断并报告 |
| IL2CPP metadata | ✅ | ✅ 仅验证过 v24/27/29/31/39 |

**边界**：已处理样本覆盖多种 Unity 版本与打包方式，但**不等于所有 Unity 游戏通用**；不可汉化的（运行时拼接/加密/服务器下发/贴图文字/位图字体）明确阻断不假报；字体注入仅限 Windows x64 Mono。

### 字体（口口乱码）说明

替换字体 + 完整性验证链：写入游戏的字体需通过逐码点验证，缺失码点如实报告而非静默替换——防止「汉化了但全是口口」的假成功。

## 8. 当前局限（客观）

> 本项目仍处于早期阶段，以下问题如实告知，请按需评估：

- **识别不全**：拼接/加密/服务器下发/贴图内文字无法识别（需 OCR）；未知文本形态可能漏识别（有告警留档，但不保证全覆盖）——部分游戏的文本可能提不全
- **翻译质量不高**：1.8B 属小模型，复杂句/文学性表达质量有限；多义词易错译——4B 审核能拦一部分语义错译，但误判与漏判并存，需人工确认兜底，译文整体质量与专业汉化组有差距
- **写回可能有 bug**：同类未知引擎配置仍有风险；固定容量写回译文过长会截断；写回后**按键 UI 失灵、游戏卡住无法推进**等逻辑性问题可能发生（已有多层防护——对象名保护/事件绑定保护/写回前逻辑审计，但无法保证所有游戏零问题）——建议写回前备份原游戏文件，发现问题在审校页处理或反馈 Issue
- **验证局限**：闭环验证到「重开比对」为止，不做实机测试——UI 溢出、字体渲染、按键失灵等运行期问题可能漏检

## 9. 贡献指南

### 9.1 一个人的项目，期待更多双眼睛

> 本项目由作者**一人独立开发与维护**，没有外部合作者。但这不代表封闭——恰恰相反：一个人的项目终究有盲区，多一双眼睛，就少一处漏网。

如果你对这个项目感兴趣，以下任何一种参与都无比珍贵：

| 参与方式 | 适合谁 | 怎么做 |
|---|---|---|
| 反馈真实样本 | 任何用过工具的人 | 提交 Issue：附游戏版本、文本形态、出错环节 |
| 提交问题 | 遇到 bug 的用户 | Issue 带复现步骤 + 日志 |
| 补测试/文档 | 有开发经验的伙伴 | Fork → 本地开发 → 提交 Pull Request |
| 参与核心开发 | 想长期一起做的人 | 联系作者（见[第 11 节](#11-联系信息与致谢)） |

### 9.2 如何为项目做出贡献

1. **了解项目** —— 阅读 README 与 `docs/` 设计文档，理解目标、架构与设计原则
2. **找到贡献机会** —— 查看 Issues：可修复的缺陷、待实现的计划项（见[第 12 节](#12-未来计划)）
3. **贡献代码** —— Fork → 本地开发 → 提交 Pull Request

### 9.3 代码提交标准

- 代码必须符合项目现有风格（注释密度、命名、结构对齐）
- **提交的代码必须通过全量测试**（`python -m pytest -q`）
- 新功能必须附带测试（2800+ 测试是质量保障的基石）
- 提交信息遵循现有规范（`feat(core):` / `fix(quality):` / `docs(record):` 等前缀 + 中文描述）

### 9.4 提交问题与拉取请求的流程

| 方面 | 提交问题（Issue） | 提交拉取请求（PR） |
|---|---|---|
| 标题 | 明确、具体（如「质量门误判：XX 文本被拦截」） | 清晰、描述改动目的 |
| 描述 | 复现步骤 + 预期/实际行为 + 相关日志 | 改动内容 + 必要性 + 测试结果 |
| 附加 | 游戏样本特征（版本/形态）、截图 | 说明测试覆盖情况 |

## 10. 许可证

### 10.1 项目许可声明

本项目当前以个人工具形式维护，源码许可待公开分发时正式确定（届时随仓库提供 `LICENSE` 文件）；在此之前，请在获得作者明确许可后使用、修改与再分发。

### 10.2 开源组件与许可

本工具基于以下开源组件构建（均允许自由使用与再分发；发行包已包含对应许可文本）：

| 组件 | 许可 |
|---|---|
| llama.cpp（推理运行时） | MIT |
| Qwen3 系列模型（4B/0.6B） | Apache-2.0 |
| UnityPy（资产解析） | MIT |
| Il2CppDumper（IL2CPP 元数据） | MIT |
| bmfont（位图字体生成） | 免费软件 |
| PySide6 / Qt（界面） | LGPL-3.0 |
| Noto Serif CJK SC（中文字体回退链，单字体收敛） | SIL OFL-1.1 |

> **合规说明**：本工具不包含、不复制任何游戏的资源与文本；汉化产物仅针对用户本机拥有的游戏副本。实测游戏均为用户本机自购/免费副本，本仓库不提供、不传播任何游戏本体或破解内容；各游戏文本样本与闭环记录仅保留在本地，不入仓库。

## 11. 联系信息与致谢

### 11.1 联系信息

如有问题、建议，或**希望加入项目一起开发**，欢迎通过以下渠道联系：

| 渠道 | 说明 |
|---|---|
| 交流群 **931708916** | QQ 群，日常交流、问题互助、测试反馈首选 |
| 本仓库 Issues | 附复现步骤与相关日志（bug 反馈） |

### 11.2 致谢

本项目由作者一人独立开发。每一轮地毯式排查的修复都来自真实游戏样本的实证反馈——感谢这些游戏的开发者：没有它们，就不会有今天的质量门与修复清单。

**期待加入**：无论你是想反馈一个真实样本、补一条测试、写一段文档，还是参与核心功能开发，都欢迎联系作者。一个人的项目终究有盲区，多一双眼睛，就少一处漏网。

## 12. 未来计划

**近期（0.3x）——核心功能聚焦**

1. **识别能力增强**：运行时拼接/动态文本跟踪识别、Addressables 深层扫描补强、贴图内文字 OCR 通道、未知形态告警改进
2. **写回可靠性**：引擎配置形态清单化保护、固定容量截断改进、重开验证覆盖面扩展
3. **翻译质量**：审校反馈重译链优化、术语库/语境库注入增强

**中期**

- 多语言对支持（当前英→中，扩展日→中、韩→中）
- 更大翻译模型档位（7B 级，按硬件自动选择）
- 自动截图比对验证（替代实机测试）
- 翻译文件导入/导出（CSV/JSON），对接汉化组协作与人工校对工作流

**远期**

- GitHub 发布页 + 自动更新检查（含完整许可清单随包分发，届时确定本项目 LICENSE）
- 术语库/语境库社区共享通道（匿名化导出）
- 插件化工具链（第三方提取/写回插件接口）

> 计划按实证与用户反馈滚动调整。

## 13. 文档导航

完整设计文档与实施记录见[本仓库 `docs/` 目录](https://github.com/mingminghh611/UL10nForge/tree/main/docs)（发行包不含开发文档）：

| 文档 | 内容 |
|---|---|
| 全面升级执行指令 | 四模型+审核闭环+UI 重构的执行记录 |
| 地毯式排查升级计划 | 排查流程与游戏闭环记录（F1-F25+ 修复清单） |
| 翻译质量保障系统实施计划 | 质量体系设计与审核闭环架构 |
| 知识库体系 · 记录模板规范 | 知识沉淀与记录标准 |

## 14. 版本

**0.36.0**（2026-08-26）：质量门全面修复升级——① 专有名词回显根除：品牌/工作室名（如 "Out of the Loop studio"）在本地 1.8B 英文提示词下回显原文被质量门 target_script_mismatch 误杀的问题，批量翻译链新增中文指令两步兜底（整句指令→逐词指令，经 `translate_source_directive` + 回显剥离 + 质量门自愈后落库），重译反馈链路 `retranslate_with_feedback` 同步补齐；② 审校放行链路修复：failed 条目带 `proper_name` 回显豁免标记且 4B 判 PASS 时不再误阻断（translator=None 的 GUI 单条审校路径也放行），译文保留非 BLOCKED；③ 多语言源保护：西里尔/假名等源（Clé en Fer 等）不落入中文指令兜底，仍走双跳/对象引用/多语保留链；④ 新增 2 个回归测试固化回显→指令兜底行为（254 批译 + 159 审校全绿）· **0.35.0**（2026-08-22）：设置与术语库 UI 重做 + 游戏语境 + 稳定性修复——① 设置页重构（信息分级/区块留白/说明文案独立成第五个分类「说明」集中收纳，各功能页文档文本移除）；② 术语库页重做（人性化布局：分组/预览/快捷引用）；③ 游戏语境（game_context：术语/对象名/人称等语境自动注入翻译，配套对话框 UI）；④ 稳定性——审校 TRANSPORT_ERROR 根因修复（中文 Windows 下 netstat 输出 GBK 解码崩溃 → errors="replace" 兜底）、写回复制误拒修复（Windows ctime=目录项创建时间，复制即变 → 大文件指纹改 size+mtime 双因子，复现 4 文件 diff → 0）；⑤ 真机全流程实测（dead-catch：扫描 7s / 翻译 182 条 53s 0 失败 / 审校 176 条 / 写回 174 条 PASS，产物含中文文本验证）· **0.34.0**（2026-08-21）：翻译工作台体验统一——① 快译小工具提示词回显根治（纯符号 `{}`【】输入翻译前拦截不调模型 + 终末回显护栏，去空白归一后整段仍是提示词片段即判空）；② 翻译页运行区重做为左右双栏（实时处理流 + 运行记录，可收起腾位）；③ 全链路进度条 3-3-3-1 统一（翻译 0-30% / 审校判定 30-60% / 审校处置 60-90% / 写回 90-100%，单调不倒退 `_pipeline_floor`，审校处置段经新增 `on_disposition_progress` 回调驱动——原本串行重译 2-30s/条期间界面静止数分钟）；④ 配色与动效统一（实时处理流状态按阶段语义着色 idle/running/succeeded/warning/error、质量门失败原因胶囊高亮、图例尺跨段淡入淡出 220ms OutCubic，全部经 `motion_enabled()` 门控、reduced-motion 直切，进度条值同步可断言不动画）· **0.33.0**（2026-08-20）：体验四连——扫描进度条（百分比+渐变色实时显示）、概览页任务流水线五格等宽重做、审校页来源文件筛选器（译文一多按文件聚焦）、游戏档案卡重做（渐变强调卡+图标，档案字段直译提示词链路核实）与档案精简（删除与「翻译风格要求」重复且无 UI 入口的旧 style_guide 字段，旧库残留自动忽略）· **0.32.0**（2026-08-20）：性能与安全大修——扫描六根因修复（O(N²) 聚集/2 万次 DB commit/GUI GIL 饿死，实测 1557s → 50s、峰值内存 9.4GB → 316MB）、翻译写回链路性能复盘、写回安全审计 8 项修复、GUI 多游戏目录选择框（多 Unity 玩家布局此前不可用）、跳过率告警（识别哑信号可见化）· **0.31.0**（2026-08-18）：Rendezvous 字体链路加固 + 单字体收敛——多容器补丁目标定位（Mono 游戏 sharedassets 实证）、TMP SDF bundle 运行时兜底（按 Unity 版本部署，保留原字体 fallback）、字体收敛为单一 Noto Serif CJK SC Medium（删除多字体/多档位，重打包 4 个 Unity 版本 bundle）、GUI 移除字体档位设置 · **0.30.0**（2026-08-15，首个公开测试版）：识别/翻译/审校/写回/发布五阶段流水线重做、审校管线四根因修复（术语提示污染/一致性杀重译/多行候选/批审幻觉，重译收敛率 45%→85%）、对象名保护（「no translation found」类断引用修复）、写回字体门默认候选放行（免实机闭环）、翻译工具页纯翻译精简、审校页 AI 翻译填充、批量翻译进度按真实条数汇报 · 0.26.x：四模型本地推理 + 审核闭环 + 语境库/向量/重排 + UI 重构 · 0.25.x：第九轮排查 + AgentMemory 上线 · 0.24.x：写回校验闸门与识别证据分层 · 0.22.x：语义审核基础设施 · 0.20.x：六库知识库 · 0.11.0：发行包内置 Python 3.12，免安装
